README
======

This package includes the following files.

|-- Consonants.c [This program counts all the consonants of the line]
|-- Vowels.c [This program counts all the vowels of the line]
|-- Other.c [This program counts all the other of the line]
|-- Initiator [This program initiates all the forks and proccess the returns]
|-- README.txt [This file]

Use the provided makefile as a template.

To compile:
    type make -- the executables will be created

To run:
	Initiator [file]    
For example;
    Initiator textfile.txt

